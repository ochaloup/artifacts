package io.narayana;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeLRAParticipantResourceIT extends LRAParticipantResourceTest {

    // Execute the same tests but in native mode.
}